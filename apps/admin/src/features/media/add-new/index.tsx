import { FormCard } from "@/components/form-card";
import { MediaForm } from "../components/MediaForm";

const CreateMedia = () => {
  return (
    <FormCard title="Add New Category">
      <MediaForm />
    </FormCard>
  );
};

export { CreateMedia };
